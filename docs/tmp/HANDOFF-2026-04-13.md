# Handoff — 2026-04-13 23:00 EDT

## What was done this session

### Fixed: Agent dispatch pipeline
The core problem was that agents were never actually running. The pipeline created cubic metadata but nothing spawned agent processes. Fixed the full chain:

**Commits (in order):**
1. `f270574` — P168: audit_log column mapping (actor→changed_by, etc.) + approved decision logging
2. `9e19cdc` — P169: removed spawnAgent (WRONG — this broke the machine)
3. `2ee0089` — Fixed infinite re-enqueue loop (fn_enqueue_mature_proposals only on initial drains)
4. `72ac457` — P167: gate_decision_log inserts on every gate run
5. `75ac11e` — Reverted P169: restored spawnAgent, subscription auth (no API key)
6. `5038c0f` — Switched spawner to hermes CLI with Nous provider
7. `05c54f5` — PATH includes /home/andy/.local/bin for hermes CLI
8. `8f54846` — Auto-detect available CLI, fallback to hermes (claude→hermes if not logged in)
9. `8705f1e` — Heartbeat-based liveness (output=heartbeat, kill if silent 2 min)

### Fixed: fn_enqueue_mature_proposals
Added check for recently done transitions (1 hour window) to prevent infinite re-enqueue loops. Also only called on initial drains, not coalesced re-drains.

### Fixed: P149 status
P149 was stuck: workflows table showed COMPLETE but proposal table had DEVELOP. Updated proposal to COMPLETE. This stopped the infinite transition churn.

### Proposal created: P228
Cubic Runtime Abstraction — multi-CLI support, host auth inheritance, cross-host A2A, orchestrator-level model selection.

### Direct DB writes (MISTAKE)
Wrote skeptic reviews directly to DB for P178-P185, P199 instead of using MCP. Cleaned up (deleted reviews, reset ACs). These proposals still need proper MCP review.

## Current state of the machine

### What works
- Pipeline claims transitions → dispatches agent → agent runs → transition marked done
- Auto-detect: tries claude first, falls back to hermes (Nous) if not authenticated
- Heartbeat liveness: agent output keeps it alive, killed only if silent 2 min
- 19 agents registered in agent_registry (claude/one, claude/andy, xiaomi, codex, hermes-agent, etc.)

### What's still broken
1. **Agents don't actually change proposal state** — they load skills, start evaluating, but timeout or exit without calling prop_transition. The gate review task is complex for a single hermes chat invocation.
2. **P169 still in DEVELOP** — has been processed ~10 times, agent keeps evaluating but never advances.
3. **7 REVIEW proposals need proper MCP review** (P178-P185, P199) — I cleaned up my direct DB writes but didn't redo them properly.
4. **P168/P167 set to 'mature' then 'new'** to stop loops — their code fixes are done but they haven't been through proper gate.
5. **4 Pillars (P046/P047/P048) have 75 ACs with 0 passing** — nothing has been worked.

### Services
```
hermes-gate-pipeline.service — active, running
hermes-orchestrator.service   — active, running (dispatches on pg_notify)
MCP server                    — http://127.0.0.1:6421
```

### DB credentials
```
PG_HOST=127.0.0.1 PG_USER=admin PG_PASSWORD=YMA3peHGLi6shUTr PG_DATABASE=agenthive
Proposal schema: roadmap_proposal
Transition schema: roadmap
Agent runs schema: roadmap_workforce
```

### Key files
```
src/core/orchestration/agent-spawner.ts  — spawnAgent, CLI builders, heartbeat liveness
src/core/pipeline/pipeline-cron.ts       — transition queue worker, cubic dispatch
scripts/orchestrator-with-skeptic.ts     — skeptic gate, audit_log, gate_decision_log
```

## Next steps for Claude

1. **Figure out why agents don't call prop_transition** — the hermes chat agents load skills and start evaluating but never complete the gate review. The task prompt might be too complex, or the agent doesn't have MCP tool access to call prop_transition/verify_ac.

2. **Simplify gate task prompts** — the current gate_task_templates are detailed multi-step instructions. A single hermes chat -q invocation might need a simpler, more focused prompt.

3. **Proper MCP review of 7 REVIEW proposals** — use claim_proposal, prop_get, submit_review through MCP, not direct DB writes.

4. **Advance P167/P168** — their code fixes are committed. Need proper gate evaluation.

5. **Start working P046/P047/P048 ACs** — these are the 4 pillars with 75 pending ACs.

## Architecture notes

- Auth is HOST responsibility. AgentHive doesn't manage API keys. Each CLI (claude, codex, hermes) reads auth from its own config in HOME.
- The cubic is a metadata container + locking mechanism. The actual execution is in agent-spawner.ts which spawns a CLI subprocess.
- P228 filed for cubic runtime abstraction enhancement — multi-CLI, cross-host, model selection.
- `claude` CLI is not logged in on this host. `hermes` (Nous subscription via portal.nousresearch.com) is the working runtime.
