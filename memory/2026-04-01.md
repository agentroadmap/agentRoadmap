# 2026-04-01 Daily Log

## Proposal System Setup
- Created 26+ RFC proposals for major components
- Duplicate proposal-004 cleaned up

## Team Conventions Established

### Proposal Flow (CRITICAL)
- **RULE**: Create proposals via MCP → SDB FIRST, then export to markdown for Git backup
- Do NOT write proposals to markdown files without creating in SDB first
- GQ77 reminded team: proposals must go through MCP to SDB
- Markdown files are just export/backup format

### Git Push Convention
- **Gilbert** handles git push operations (convention, not hard rule)
- Others can push if needed, but defer to Gilbert as Git Workflow Master

### Config Structure
- Config split: init-time values vs runtime values
- Component metadata stays in SDB (not config)
- Config design tracked as RFC-20260401-CONFIG-REDESIGN.md

### Primary Key Strategy
- SpacetimeDB auto-increment for Proposal struct
- `id` = auto-generated, `display_id` = human-readable
- No manual ID setting needed
