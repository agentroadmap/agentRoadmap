# AgentHive - 2026-04-04 Session

## GQ77 Directives (16:27 EDT)
1. Get access to Postgres databases → **DONE**
2. Modify AgentHive MCP to use new Postgres databases: agenthive → **IN PROGRESS**
3. Design better tables in Postgres DB to reflect docs → **DONE**

## Phase 1-2: Postgres Setup ✅
- Found existing `postgres-db` container (PostgreSQL 16, port 5432)
- User: `admin`, Password: `<PG_PASSWORD_REDACTED>`, DB: `app_db`
- Created `agenthive` database
- Created 11 tables: `proposal`, `proposal_version`, `attachment_registry`, `agent_registry`, `team`, `team_member`, `agent_memory`, `message_ledger`, `model_metadata`, `spending_caps`, `spending_log`
- Created 8 indexes for common queries
- Seeded: 5 agents (Andy, Bob, Carter, Gilbert, Skeptic) + 3 models

## Phase 3: MCP Postgres Integration 🔄
- Installed `pg` + `@types/pg` packages
- Updated `config.yaml` → port 5432, database: agenthive, user: admin
- Created `src/postgres/pool.ts` — Pg connection pool
- Created `src/postgres/proposal-storage.ts` — Full CRUD adapter
- Committed: `a397905 feat(postgres): add Postgres integration layer`
- **NEXT**: Swap sdb-* handlers → pg-* handlers in MCP tools

## Issues
- pgvector extension NOT available in postgres:16 container
- `body_embedding` stored as TEXT for now
- Need to upgrade to `pgvector/pgvector:16` container later

## Worktree
- `/data/code/AgentHive-carter` on `feature/carter-work`
- Commit: a397905
